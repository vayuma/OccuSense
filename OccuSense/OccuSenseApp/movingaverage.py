import requests
response = requests.get('https://techtutorialsx.com/2016/10/30/python-parsing-json/')
print(response.text)









def getAvg(x, size, total): 
    total = total + x; 
    return float(sum) / n; 
  

def streamAvg(array, n): 
	n = len(array)
    avg = 0; 
    total = 0; 
    for i in range(n): 
        avg = getAvg(arr[i], i + 1, total); 
        total = avg * (i + 1); 
        print("Average of ", end = ""); 
        print(i + 1, end = ""); 
        print(" numbers is ", end = ""); 
        print(avg)
    return; 